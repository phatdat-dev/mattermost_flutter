name: mattermost_example
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: "3.8.0"
  flutter: "3.32.0"

dependencies:
  flutter:
    sdk: flutter
  package_info_plus: ^8.3.0

  mattermost_flutter:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any

flutter:
  uses-material-design: true
